{
  "name": "@360solve/contracts",
  "version": "1.0.0",
  "private": true,
  "description": "OpenAPI contracts and generated types",
  "scripts": {
    "generate": "openapi-generator-cli generate -i openapi/v1.yaml -g typescript-axios -o generated",
    "validate": "swagger-cli validate openapi/v1.yaml"
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.7.0",
    "swagger-cli": "^4.0.4"
  }
}
